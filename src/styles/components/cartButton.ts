import { styled } from "..";

export const CartButtonContainer = styled('button', {
    background: "$gray800",
    borderRadius: 6,
    padding: '0.75rem',
    border: 0,
    cursor: 'pointer',
    display: 'flex',
    height: '100%',
});